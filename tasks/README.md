# Task breakdown

Drafted during the design phase. **Nothing here is assigned and nothing is
started** — task assignment is itself subject to cross-review, per the owner's
instruction. Status vocabulary:

- `BLOCKED` — a dependency or an owner decision is outstanding.
- `READY` — dependencies met; may be assigned.
- `WIP` / `DONE` — claimed / merged.

Everything is `BLOCKED` until this design PR merges. Numbers are stable from the
merge of this PR onward; dependencies are by number.

**Revision 14** adds no rows and rewrites seven, all at seams rev 10's host move
left unchecked. The ones that change *what a task must do*, rather than how it is
described:

- **`08` moves to the Mac and its gate moves earlier.** The lifetime slot is
  spent when **Link succeeds**, not at the exchange (`DESIGN.md` **F2a**), so a
  confirmation prompt before the paste was asking a question that could no longer
  change anything. And the pre-Link check is now a **canary through the real
  backup path** — build, pull, decrypt, delete — instead of a reachability ping.
- **`03a` inverts:** `zelengs-macbook-air-2` **pulls**; the VPS only builds.
  The archive must be a *coherent* snapshot (`VACUUM INTO` under a lock shared
  with token writes), the drill checks that **every `item` row resolves to a
  token in the same archive**, and the runtime key is `/etc/networth/`, not a
  directory on another computer.
- **`20` splits and grows a deployment.** The receiver becomes its own unit
  (`networth-hook.service`) so the phone's route cannot be killed by a public
  input; the endpoint is **Tailscale Funnel**, proven by a **live Sandbox
  webhook**; and the task is not done until every existing Item has been through
  `/item/webhook/update`.
- **`03` gains two tables** — `published_envelope` and `backup_archive` — because
  §6.3.1 and §14a both claimed to store something §7 had nowhere to put.
- **`06` gets its own credentials and its own database.** Sandbox and Production
  are separate credential sets; `NETWORTH_ENV` selects file *and* database and
  fails closed, so a rehearsal cannot reach Production.
- **`28` must not touch `PermitRootLogin`,** and rev 12's fix now reaches the
  step the owner actually runs (`DESIGN.md` §19 step 3) rather than only the
  rationale in §15.1.
- **Phase 1 and 17–20 are not "Mac-side".** They run on the VPS and have since
  rev 10; the labels were current-tense instructions pointing at the wrong host.

**Revision 13** adds nothing and corrects two things. **`00a` grew an item that
was wrongly reported as done:** the Mac has **no Tailscale installed at all**, so
O8's backup destination does not exist yet — and the way that got misreported is
worth knowing, because `tailscale status` run *from the VPS* lists two **stale
registrations** for this Mac, both offline, one of which was misread as a fresh
join. **A registry serving an entry after the thing it describes is gone, read as
current because it was there, is this project's own founding failure mode.** The
check is now positive and local. And `03a`'s backup is **opportunistic, never
daily** — the Mac sleeps, so it runs when the Mac is awake, and the system
reports **when the last successful backup actually happened** rather than
assuming the schedule fired.

**Revision 12** adds nothing and sharpens `28` with three criteria that are about
**not breaking the owner's machine**: `PermitRootLogin no` must not be applied
unattended (it is not a fresh host — he administers it as `root`, and this design
does not get to lock him out of his own exit node); `/etc/networth/` is
root-owned today, so the `chown` is reported rather than silent; and config is
read from `/etc/networth/plaid.env` with `PLAID_ENV` never hardcoded. **O2 is
also now proven end to end** rather than inferred — a live Item-free call from
the VPS returned 200 with 10,033 institutions, which incidentally is the first
test of the new host's egress to Plaid.

**Revision 11** adds nothing and unblocks a leg: **O2 = GO** (`DESIGN.md` **F4**),
so task `00` is **DONE** and `06`, `07`, `08` and the downstream Production-Link
work lose their O2 label. **`08`'s other gate does not clear with it** — 03a's
backup drill still stands ahead of it, because the ten Item slots are lifetime
whatever the plan says. `00`'s row now also carries the two things that outlive
it: the "Get production access" paid-funnel trap the owner walked into, and the
rule that **the production secret never reaches an agent**.

**Revision 10 changes this list more than any revision before it, and mostly by
deleting.** The owner answered five open questions at once, and one of them moved
the host from the Mac to a VPS he already owns (`DESIGN.md` §5, §6.2). Rows are
never renumbered — a review comment from rev 4 still has to resolve — so removed
work is struck through in place with the reason:

- **`12a` (webhook drain) — DELETED.** It was a receive route at a third party
  plus a queue, a drain loop and an ack protocol, all of which existed because
  the Mac had no public endpoint. The VPS has one. The webhook receiver is now
  two dozen lines inside `20`.
- **`07a` (automatic `public_token` handoff) — DROPPED, not deferred.** The
  browser is on the Mac and the exchanging process is on the VPS, so an automatic
  handoff would mean opening a route on the VPS for a step that happens at most
  ten times ever and takes ten seconds (§16).
- **`19` shrinks by about two-thirds.** The publication state machine, the
  expected-live set, `payload_fingerprint`, the pre-write compare and the
  read-back are gone with the third party they policed — as are the fourteen
  timeline tests that pinned them down. This is where the three blockers open
  against rev 7 lived; **they are void by removal, not by fix.**
- **`19a` and `20` are rewritten** for a single host: rotation is one local
  SQLite transaction, so there is no `PENDING`/`UNCERTAIN` state, no registration
  round-trip and no suspend-publishing rule; the transport backend is two routes
  in the daemon's own process instead of six at a third party.
- **`15` (alerts) is rewritten around the owner's channel decision**: in-app on
  the phone only. Seven alert kinds become four, and the four that remain must
  travel *inside the payload* because there is no other way to reach him.
- **`16` moves from launchd to systemd.** The battery/`KeepAlive` reasoning was
  Mac-specific and goes with it; the stored-state due-ness predicate stays,
  because that was never about launchd.
- **`03a` loses the APFS physical-store resolution** — the destination is now
  another machine by decision (**O8**), which satisfies the failure-domain
  criterion by construction. The key-escrow attestation and the restore drill
  are unchanged, and so is the hard gate on `08`.
- **`13` gains the O4 answer**: a revision log for real property, in which **a
  revision applies from its own date forward**.
- **New: `28`, VPS provisioning and hardening.** Numbered after the existing
  rows rather than inserted, so nothing renumbers.

*(Earlier revision notes, rev 2 through rev 9, are compressed rather than kept in
full: most of them recorded acceptance criteria on `12a`, `19`, `19a` and `20`
for mechanisms this revision deleted. `DESIGN.md`'s own revision log explains
why the branch was expensive enough to be worth deleting, and the full text is
in git history and on PR #1. What is worth carrying forward: rev 2 added `03a`,
`12a`, `12b` and `19a`; rev 3 added the owner gate `00a` and corrected three
dependency edges — `03a` needs `00a`, `19a` needs `20` because pairing needs a
transport to exist first, and `06` needs `00` because Sandbox needs the dashboard
account even though it does not need O2's answer; rev 4 sharpened `03a` to
resolve a **physical store** rather than a filesystem and `16` to be due on
non-market days too, both of which are still live; revs 5–8 were four rounds of
findings against the third-party transport; and rev 9 answered O6 (**Android
only**) and deferred credit cards, doubling `08`'s reserve from 2 to 4.)*

## Phase 0 — gates (owner-only, nothing can ship before these)

| # | Task | Deps | Status | Notes |
|---|---|---|---|---|
| 00 | Create the Plaid account, get the Trial plan approved, **verify the in-scope brokerages are reachable on Trial (O2)** | — | **DONE (owner, 2026-08-30)** | **O2 = GO.** Trial active at `0/10`, Production `client_id`/`secret` issued, and Plaid states bank access is **automatic on the trial** — no per-institution request (`DESIGN.md` **F4**). Tasks 07, 08 and the downstream Production-Link work are ungated; 06 has its dashboard credentials. **Two things this task leaves behind, both live:** (1) the runbook trap — "Get production access" is the **paid** funnel and the Trial is a separate plan not offered in it (`DESIGN.md` §19 step 1); the owner walked into it and stopped at the plan picker, so any future doc must not send anyone back there; (2) **the production secret must never reach an agent** — agents write the command, the owner runs it on the VPS (§15). |
| 00a | **Install the agents' keys on the VPS**, and escrow the backup key | — | BLOCKED (owner) | Runbook `DESIGN.md` §19 steps 1a–1c. **O8 is answered and rev 14 inverted it**: `zelengs-macbook-air-2` (`100.96.163.67`) **pulls** from the VPS. What is left here is owner-only *access*: only the owner can add a key to `authorized_keys`, and **no agent may ever ask him for a password** (§15.1). **Two keys now** (§15): the interactive `networth-vps.key`, and a `restrict,command=`-constrained `networth-backup-ssh.key` for the unattended pull, so a compromised laptop cannot open a shell on the host holding the Plaid master credential. Until he confirms, assume no VPS access — and do **not** block foundation work on it (28 is the only task that needs it). **The tailnet half is DONE and verified (rev 14)**: `zelengs-macbook-air-2` is Connected at `100.96.163.67`, the VPS host key matches across its public and tailnet addresses, `tailscale ping` is direct, and SSH Mac→VPS works with the peer observing `100.96.163.67`. **Rev 13's claim that this Mac had no Tailscale was one error and its "two stale registrations" reading was the opposite error** — there are **four** MacBook Airs on this tailnet and they are four different computers (differing macOS versions and IPs are the discriminating signal; the names are not). **Nothing was pruned.** Never write a bare hostname prefix into a config, unit, script or runbook step: `zelengs-macbook-air` is a *different machine* that a prefix match will silently select (§19 step 1b). Also carries the key escrow that 03a's criterion 2 attests to. |
| 01 | UI target | — | **ANSWERED** | The owner chose a **Flutter phone app**, narrowed by O6 (rev 9) to **Android only** — one deliverable, one APK, no iOS. Number retained so later references stay valid. Its consequence is Phase 4, not a task of its own. |

## Phase 1 — foundation (host side; UI- and transport-agnostic, and the bulk of the work)

| # | Task | Deps | Status | Notes |
|---|---|---|---|---|
| 02 | Project scaffold: package layout, venv, format/lint/test, CI, `scripts/check-no-secrets.sh` as a pre-commit hook and a CI job | — | BLOCKED (design PR) | Can start before 00 lands; touches no credential. The secret/figure scanner exists from the first commit because the repo is public. |
| 03 | SQLite schema + migration runner | 02 | BLOCKED | `DESIGN.md` §7 verbatim. Integer minor units, UTC timestamps, **two clocks per observation** (`fetched_at` / `source_as_of` + `source_clock`), `lineage_id` on accounts, `snapshot.sync_run_id` UNIQUE. **No `profile_id`** — dropped in review as speculative generality. **Rev 14 adds two tables, both because a section claimed to store something this schema had nowhere to put — and since this task builds §7 *verbatim*, that gap would have forced tasks 19 and 03a to invent their own storage contracts.** (a) `published_envelope`: nonce, ciphertext‖tag and the four AAD fields stored **as served**, with a **partial unique index** (`WHERE is_active = 1`) so "which envelope is current?" cannot have two answers — enforced by the database rather than by whichever writer read the spec. (b) `backup_archive`: `built_at` and `pulled_verified_at` as **separate** columns, the same two-clock discipline as observations — an archive on the VPS is not a backup of the VPS, and only the second column may answer "is there a second copy?" |
| 03a | **Encrypted archive on the VPS + a Mac-initiated pull + a passing restore drill** | 03, 05a, **00a** | BLOCKED (00a) | `DESIGN.md` §14a **and §14a.1**. **Rev 14 inverted the direction** (owner): the VPS *builds* the archive into a local directory and knows nothing about any Mac; **`zelengs-macbook-air-2` (`100.96.163.67`) pulls it.** macOS runs no `sshd` by default and the Mac sleeps, so a push would have meant opening an inbound service on the machine holding the second copy and then retrying into a sleeping laptop forever; a pull leaves the internet-facing host with **zero** new outbound trust relationships. Two build-side requirements that rev 13 did not have and that a naive `rsync` gets wrong: **(i)** the database is WAL-mode, so the archive is produced with **`VACUUM INTO`** (or the online backup API) — copying the `.db` while writes continue can omit committed data sitting in the `-wal`; **(ii)** the DB snapshot and the `TokenStore` copy happen under **one `flock` shared with token writes**, and tokens are `fsync`ed **before** the `item` row that references them — because an orphan token is harmless and recoverable while an `item` row whose `access_token` is missing strands a lifetime slot. The archive is published atomically at **both** ends — the builder renames into place too, or a puller arriving mid-build fetches a truncated file — and on receipt it is temp name → `fsync` → verify → `rename`. **Three ordinary-timeline behaviours that are acceptance criteria, not implementation detail:** a verified pull whose write-back to the VPS fails leaves `pulled_verified_at` `NULL`, which under-reports and is the correct direction for this fact to fail; **the puller re-records whenever it holds a verified archive whose row is still `NULL`, transfer or no transfer** — a puller that skips because it already has the newest file would otherwise never retry that write-back and `doctor` would under-report forever; and `link.sh`'s canary probe is built to a distinct path, deleted after it decrypts, and **never writes a `backup_archive` row**, because a rehearsal that leaves a record indistinguishable from a real backup is this project's own failure mode self-inflicted. Three acceptance criteria: **(1) a destination in a separate failure domain**, satisfied by construction — another machine. *(Rev 10 deleted the APFS `diskutil` resolution this row used to specify; moot for a remote destination, and macOS-only code that would not run on Ubuntu.)* What survives is the part that was never about disks: the check runs on **every pull** and **fails closed** — `pulled_verified_at` stays `NULL` unless the Mac holds the archive, decrypted it, and reconciled it. **The backup is opportunistic, never daily, and no acceptance criterion may imply otherwise.** Apply this project's own rule to its own backup: `doctor` and the app report **`last_successful_backup` = when a verified copy actually landed**, never `built_at` and never the schedule having fired. *What the inversion removes is the whole 'destination unreachable' error class — a sleeping laptop is no longer a failed job, just a pull that has not happened yet.* The Mac reports its verdict back over the SSH connection it already opened (`networth backup record-pull`), stamping `pulled_by` with its **full** tailnet name, because four Airs here differ only by suffix. **The puller is a `KeepAlive` LaunchAgent, never `StartInterval`** — launchd defers interval timers on battery, a sibling project on this machine proved it, and the owner's standing rule is that things work on battery with no plug-in prompt and no battery guard; **a pull observed while on battery is an acceptance test**; **(2)** `networth backup attest-key` records `key_escrow_confirmed_at` — an **attestation, not a proof**. The runtime key is **`/etc/networth/networth-backup.key`** *(rev 14 fixes a path that had drifted to `~/agents/secrets/`, a directory on a different computer that VPS code cannot read)*; the owner's escrow copy is not a second runtime location; **(3)** `scripts/restore-drill.sh` runs **on `zelengs-macbook-air-2`**, against the archive in its own destination directory — the copy a real recovery would reach for, on the machine that would still exist. It checks **the invariant, not the volume**: schema version, row counts against `backup_archive.db_row_counts_json`, and — the check rev 13 lacked — **every `item` row resolving to a token in the same archive**, by salted fingerprint, never tokens, never in a log. Matching row counts prove the database arrived and say nothing about whether it arrived paired with the right token generation, which is the exact failure the lock and the ordering exist to prevent. Orphan tokens are reported and are not a failure. Records `last_verified_restore_at`. **Hard gate on task 08:** a lost `access_token` cannot be recovered at all and strands a permanent Item slot (F2, F2a, F6), so this cannot wait for Phase 5. |
| 04 | Domain model + `Store` repositories; append-only observations/snapshots | 03 | BLOCKED | The seam everything else reads through. No I/O beyond SQLite. Snapshots append per successful run, idempotent on `sync_run_id`; nothing is edited in place. Queries never assume a fixed set of accounts (§2 reservation 2). |
| 05 | `PlaidClient` wrapper + **error taxonomy mapping** to our states | 02 | BLOCKED | The one place Plaid errors become `DEGRADED` / `NEEDS_REAUTH` / `REVOKED` (§8.2). Must cover `ITEM_LOGIN_REQUIRED`, `PENDING_EXPIRATION`, `PENDING_DISCONNECT`, `USER_PERMISSION_REVOKED`, `ITEM_NOT_FOUND`. Unit-tested against synthetic fixtures. |
| 05a | `TokenStore`: narrow interface over secret storage, mode-600 file backend | 02 | BLOCKED | §2 reservation 3. Small, but it must exist before anything reads a token, or file reads will scatter. |
| 06 | Sandbox end-to-end rehearsal of the Link flow | 05, 05a, **00** | **READY on merge** (00 is done) | **Must pass before any Production Link.** Sandbox is free and unlimited; Production slots are permanent (F2) and are spent by a successful **Link**, not by the exchange (F2a) — which is exactly why the rehearsal is worth doing properly, since Sandbox is the only place that boundary can be walked into without cost. Depends on 00 for the **dashboard credentials** — a *different* dependency from O2's answer (`DESIGN.md` §18): Sandbox rehearsal is worth doing whatever O2 says, and needs only that the account exists. **Rev 14 gives this task its own credential and its own database, and it cannot become `READY` without them.** Plaid issues **separate Sandbox and Production** `client_id`/`secret` pairs, and rev 13 named only the Production file while forbidding a second location — leaving the rehearsal no legitimate way to authenticate. So: `/etc/networth/plaid-sandbox.env` beside `plaid.env`, same mode, same owner-installs-it rule, and **no agent sees either secret**. `NETWORTH_ENV` is **required, with no default**, and selects the credential file, the items file **and the database path** together; the process asserts the selected file's `PLAID_ENV` matches and **refuses to start** otherwise. Two things that buys: a rehearsal physically cannot write into the Production history, and a Sandbox credential sitting in a file labelled production becomes a startup failure instead of a run nobody questions. |

## Phase 2 — linking (spends the scarce resource)

| # | Task | Deps | Status | Notes |
|---|---|---|---|---|
| 07 | Static OAuth redirect page + free hosting | 00 | **READY on merge** (was BLOCKED on O2) | Holds no data and no secrets. HTTPS required; `localhost` is Sandbox-only. Register the redirect URI under *Allowed redirect URIs* — **and that is the only dashboard registration this project needs.** *(Rev 14 removes 'also register the webhook URL' from this row: for Item-based products the webhook is **not** a dashboard setting. It is the `webhook` field of `/link/token/create`, changed later with `/item/webhook/update` — `DESIGN.md` §8.4a. This row was sending the owner to look for a control that does not apply.)* |
| ~~07a~~ | ~~*Spike:* automatic `public_token` handoff~~ | — | **DROPPED (rev 10)** | Not deferred. The browser is on the Mac and the exchanging process is on the VPS (§16), so an automatic handoff means opening a route on the VPS for a step that happens at most ten times in this project's life and takes ten seconds. Copy-paste was always the shipping path; it is now the only one. Number retained so review references stay valid. |
| 08 | `scripts/link.sh` — owner-run Link on `zelengs-macbook-air-2`, remote token exchange, `TokenStore` write, item record, **verified post-Link pull** | 04, 06, 07, **03a** | BLOCKED | **Rev 14 moves this script to the Mac and moves its gate one step earlier; both changes are load-bearing.** *Where it runs:* on `zelengs-macbook-air-2` (`100.96.163.67`), because the backup is a **pull** and only that machine can perform one. It reaches the VPS over SSH for every step needing the client secret, which never leaves that host; the `public_token` is piped over **stdin**, never as an argument (`ps` output). The browser step is on the same Mac. Agents never run it. *Where the gate goes:* **the lifetime slot is spent when Link succeeds, not at the exchange (F2a).** Plaid returns a `public_token` only *after* the Item exists, so rev 13's 'confirm the institution before exchanging' asked a question whose answer could no longer change anything, and its pre-Link **reachability** check proved the network rather than the backup. Replace both: confirm institution **and login before Link completes**, and run a **canary through the real path** first — build a probe archive on the VPS, pull it, decrypt it, delete it — refusing to mint a link token if any of that fails. A full disk, an unreadable key or a missing destination directory all answer a ping and fail a restore. *The timeline this task must implement, not just the happy path* (`DESIGN.md` §14a.1): after Link succeeds the slot is gone; a lost or expired `public_token` (~30 min) strands it **permanently and unrecoverably**, so the CLI must say so at the prompt rather than treating the paste as a formality; the exchange result is `fsync`ed through `TokenStore` **before** the `item` row that references it, so a crash cannot produce the unrecoverable direction; and the script reports the Link complete only once a **verified** archive is on the Mac's disk. **Cannot start until 03a's drill passes.** **O2 clearing does not change this** — the ten slots are lifetime whatever the plan says, so GO ungated *how* accounts get linked, not *when*. **Items created before task 20 exists get no webhook, deliberately and visibly:** when the URL is unset the link token carries none, and `doctor` reports 'N Items with no webhook registered' until 20 backfills them (§8.4a). |
| 09 | `scripts/relink.sh` — Link **update mode** for an existing item | 08 | BLOCKED | Consumes no slot. The recovery path for `NEEDS_REAUTH`; the naive remove-and-relink must never be implemented (F2). |

## Phase 3 — sync and the honesty machinery

| # | Task | Deps | Status | Notes |
|---|---|---|---|---|
| 10 | Item health poller (`/item/get`, hourly) | 04, 05 | BLOCKED | The **floor** for I3 — every Item error state visible to `/item/get`, within an hour, with zero infrastructure. Also records `status.investments.last_successful_update`, which feeds the holdings source clock (§8.1). |
| 11 | `StalenessMachine`: **two axes** — connection state per Item, data freshness per account from source clocks | 04, 10 | BLOCKED | `DESIGN.md` §8.1–§8.2, §9.2. Heavily unit-tested — **this is the product**. Required cases: a call that succeeds while `source_as_of` never advances (the frozen-number failure, and the reason this task exists); `UNKNOWN` freshness never rendering as fresh; weekends, holidays and half-days on the market-close clock; a carried-forward observation continuing to age. **Axis B escalates:** merely behind its expectation window is `WAITING` (institutions post late constantly — alerting there burns the signal on noise), but Item `HEALTHY` + source clock frozen for **five consecutive market days** becomes the named `FROZEN` state, which is `ACTION_NEEDED` on screen *and* alerts. The threshold is **read from the single definition in §11**, not re-declared here or in 15 — the two carrying their own copies is what made the display and the runbook contradict each other in rev 2. **`UNKNOWN` never escalates**: under `balance_mode: cached` it is permanent, no re-link can conjure a clock, so it stays a standing caveat rather than a wait. |
| 12 | Full sync: holdings + balances → observations; carry-forward on failure | 04, 05, 11 | BLOCKED | Records `fetched_at` and `source_as_of` **separately**, plus which evidence produced the latter. Holdings take the oldest contributing `institution_price_*`; balances use `/accounts/balance/get` under `balance_mode: realtime` (F5) and are `UNKNOWN` under `cached`. Sets `is_carried_forward` honestly; a carried-forward row never advances its source clock. |
| ~~12a~~ | ~~Webhook drain: third-party receive route + queue + host-side JWT verification~~ | — | **DELETED (rev 10)** | The queue, the drain loop, the ack protocol, the TTL and the `drain stalled` alert all existed because the Mac had no public endpoint. The VPS has one, so the receiver **verifies inline and writes straight to the local database** — that is now part of task 20, not a task. Two hard-won constraints move with it and must not be lost: the raw request bytes are hashed **without re-serializing** (`request_body_sha256` is whitespace-sensitive, so a receiver that round-trips the JSON breaks every signature and makes healthy traffic look like an attack), and `UNIQUE(body_sha256, jwt_iat)` makes Plaid's retries a no-op. `DESIGN.md` §8.4. |
| 12b | Replacement-Item reconcile flow (`scripts/reconcile.sh`) | 04, 09 | BLOCKED | `DESIGN.md` §8.5. New accounts start `NEW` and contribute nothing; the owner confirms an old→new mapping; `lineage_id` carries history across the seam. Tests must cover the two silent disasters: double-counting and a severed curve. |
| 13 | Manual assets: **real-property revision log** + share count × live quote | 04 | BLOCKED | **O4 is answered: a revision log** (`DESIGN.md` §12), defaulting to the purchase price. The acceptance criterion is the part that is easy to get wrong: **a revision applies from its own date forward and the curve behind it does not move** — the value used for a given day is the latest revision *as of that day*, never the latest revision outright. Mechanically it is a new append-only observation with its own `source_as_of`, never an `UPDATE`; a test must assert that revaluing the property in 2026 leaves the 2024 points on the curve unchanged. Note *why* that test passes: `snapshot.total_net_worth_minor` is a **stored** number, not a query re-evaluated at render time — so "recompute the curve from observations", which is the obvious implementation of task 23, would reintroduce exactly the retroactive deformation this decision rules out. `QuoteClient` reuses the quotes integration from the sibling project; the quote must carry its own `as_of` and that `as_of` is the source clock. Ticker and quantities are configured at runtime, never hardcoded. |
| 14 | Snapshotter + net-worth computation + presentation contract | 12, 13 | BLOCKED | A total cannot be constructed without its **age state** + staleness counts (**I2**) — enforce it in the type, not by convention: the total is a sum type (`Dated(as_of) | Undated(reason)`), so rendering code cannot reach a date that does not exist (`DESIGN.md` §10.3b). The age is the tagged `(age_state, as_of)` of §8.1 **R3** over the *age basis* — never `oldest_contributing_source_as_of`, which was the rev-2 scalar and could not be filled honestly. Required tests: **all contributors known** → `KNOWN` with the oldest source clock; **mixed known + unknown** → `UNKNOWN` with **no date** (the case that must never borrow the oldest known timestamp); **all unknown** → `UNKNOWN`; **fixed-value assets only** → `STATIC_ONLY`; a `MANUAL_STATIC` property **never** dragging the headline age to its purchase date. Stale and `UNKNOWN` counts stay separate; `static_account_count` is its own count; unreconciled accounts contribute nothing and force `is_complete = FALSE`. |
| 15 | Alerts: `alert` table, **payload-carried delivery**, phone-local notification, anti-fatigue policy | 11 | BLOCKED | `DESIGN.md` §11. State written before it travels, so a crash re-raises rather than losing the alert. **The owner's channel decision constrains this task rather than configuring it: in-app on the phone only** — he declined email and the agent-mailbox route, and there is no Mac in the path any more. So alert state is **serialized into the payload**; the app renders it; a cached payload carries the alerts that were true when it was published, labelled as of that time. Four kinds: `NEEDS_REAUTH`, `REVOKED`, **frozen data** (Item healthy, source clock stuck — threshold owned by `DESIGN.md` §11 and shared with task 11, **not** re-declared here), and **pending reconciliation**. *(Rev 10 deleted seven: read-back mismatch, read-back unavailable, pre-write rollback, foreign write, snapshot missing, pairing uncertain and drain stalled. Each watched a third party or a queue that no longer exists — they are not "unimplemented", there is nothing left for them to observe.)* **The acceptance criterion this task must not fudge:** *publication overdue* cannot reach the phone, because the failure of the publish path is what prevents its own alert from being delivered. The row exists for `doctor`; what actually surfaces it to the owner is the phone noticing its own copy has aged past `stale_after` with reason `HOST_NOT_PUBLISHING` (§9.1) — which the app computes **without the host's help**, and which task 22 owns. Do not let a reader of this row assume the alert arrives. **A rejected downgrade is deliberately NOT here** — it is phone-local (task 22); promising the host an alert about an event only the phone can observe would need a phone→host channel the architecture does not have and should not grow. Anti-fatigue matters *more* on a single-channel design: one alert per item per state entry, re-raised at most once per 24h, never for `DEGRADED` or routine in-window staleness. |
| 16 | **systemd units + timer** + due-ness engine + catch-up after downtime | 10, 12, 14, **15** | BLOCKED | `DESIGN.md` §13. **Three units, not two** *(rev 14)*: `networth-sync.timer`/`.service` (periodic, read-write), `networth-serve.service` (`Restart=always`; the tailnet snapshot server, database **read-only**) and `networth-hook.service` (`Restart=always`; the public webhook receiver, read-write, `webhook_event` only). **The split is a correctness fix, not tidiness:** rev 13 had §8.4 writing events inline in the same process §13 opened read-only — both could not hold. Splitting makes the isolation this task already demanded as a *property* (`no request to /hook can terminate the process the phone depends on`) hold **structurally**, since that is now a different process. All three run as the **same** dedicated unprivileged service user, never root — and that sameness is deliberate: a WAL reader must write the `-shm` index, so giving `networth-serve` its own user with no write access to the database directory yields a service that cannot read at all, failing as `SQLITE_CANTOPEN` exactly when the phone asks. The read-only guarantee is the open mode (`file:…?mode=ro`), not filesystem permissions. **Two writing processes means the WAL rules are acceptance criteria, not assumptions:** `journal_mode=WAL` and `busy_timeout` on every connection; the receiver's write is a **single-row insert in its own short transaction** and never spans a network read; the sync worker's long work sits outside its write transactions; `SQLITE_BUSY` on the receiver logs and returns 5xx, which is correct — Plaid retries and webhooks are advisory. Rev 13's 'one writer' claim is retired. *(Rev 10: this was launchd `KeepAlive` with an explicit ban on battery guards — **entirely Mac-specific**, and a VPS has no battery. Note that rev 14 brings that knowledge **back into scope on the Mac side**: the backup puller of 03a is a LaunchAgent and must be `KeepAlive`, never `StartInterval`.)* **What carries over is the part that was never about launchd: due-ness is computed from stored state** — `Persistent=true` plus a stored-state predicate, so a host down for two days finds work due on boot. Depends on 15 because I3 promises an *alert* within an hour, not a recorded state. **The full-sync predicate is `min(market close + 1h, 20h since last success)`**: a market-only rule left a Friday success satisfying dueness all weekend, so balances — which move on wall-clock time — crossed their own 36h staleness window by construction. Required acceptance tests: **a Saturday and a Sunday each become due after a successful Friday run**; a holiday Monday is due; the 36h window is never crossed on a weekend by construction; a failed run followed by 1h/2h/4h/8h backoff still lands inside the day; a weekend run whose holdings did not move does **not** advance `source_as_of` and does **not** count toward the market-day `FROZEN` counter. **No webhook-drain job**, and **no backup job** — the VPS only *builds* an archive (>24h since the last build); `zelengs-macbook-air-2` decides when to pull, so this scheduler no longer models a laptop's sleep schedule. **Two details a straightforward unit file gets wrong** (§13): all units **enabled at boot** — 'always awake' is a claim about the host and buys nothing if a reboot leaves the daemon stopped; and `networth-serve` orders `After=tailscaled.service` and **retries the tailnet bind rather than falling back to `0.0.0.0`** (that fallback is the one mistake here that silently publishes a private endpoint, and 'the address was not ready yet' is exactly the plausible reason someone adds it). |

## Phase 4 — getting the number onto the phone

The half of the project the UI decision created. **17–20 run on the VPS** — they
were labelled "Mac-side" through rev 13, which stopped being true when rev 10
moved the host and was left standing as a current-tense instruction. 21–24 are
the app.
the app.

| # | Task | Deps | Status | Notes |
|---|---|---|---|---|
| 17 | `NetWorthQuery` read layer (totals, history, per-account staleness) | 14 | BLOCKED | Pure reads. The only surface a UI or a payload builder may touch. History joins on `lineage_id` so a re-link does not break the curve. |
| 18 | CLI: `networth show` / `history` / `doctor` | 17 | BLOCKED | Pure reads, run over SSH — there is no other interface on this host. First consumer; proves the seam before a phone exists, and the first place the tagged age of task 14 is rendered, so `show` must be able to print a total with **no date** without reaching for one. `doctor` prints both clocks per account, item states, remaining Item slots, days since the last verified restore drill, **`last_successful_backup` — read from `backup_archive.pulled_verified_at`, never from `built_at` and never from the schedule** (`zelengs-macbook-air-2` is not always on, and since rev 14 it is the side that initiates, so this number growing silently is the *only* signal that it has stopped pulling — the VPS has no way to notice and must not pretend to), **the count of Items with no webhook registered** (§8.4a — nonzero until task 20 backfills them), **`key_escrow_confirmed_at` labelled as the owner's own attestation** rather than a verified fact (§14a.1), and the age of the last successful publication. **`doctor` matters more than it did in rev 9**: with alerts in-app only, this is the owner's only diagnostic surface once the app tells him something is wrong (§11). |
| 19 | Payload schema (versioned) + `Publisher`: serialize → AES-GCM encrypt → store, with a `publication` row | 17 | BLOCKED | The host↔phone contract. **Rev 10 cut roughly two-thirds of this task.** Deleted with the third-party transport: the publication outcome state machine (`UNCONFIRMED`/`LANDED`/`NOT_LANDED`/`SUPERSEDED`), the expected-live set, `payload_fingerprint`, the pre-write compare and its `ROLLBACK`/`FOREIGN`/`ABSENT`/`UNAVAILABLE` classifier, the read-back, and the fourteen timeline tests that pinned all of it down. **The three blockers open against rev 7 lived here and are void by removal, not by fix.** Publishing is now a local SQLite transaction; re-reading the row you just wrote to confirm SQLite wrote it tests nothing but SQLite (`DESIGN.md` §6.4, §9.3). What remains: the payload carries `published_at`, `publish_interval_seconds`, `grace_seconds` and a monotonic `seq`; `schema_version`, `pairing_id`, `seq` and `published_at` go in the AAD, in the **envelope and canonical length-delimited AAD encoding of §6.1**, because both ends must build those bytes identically or nothing decrypts. **The payload carries the total's age as the tagged `(age_state, as_of)` of task 14** — `as_of` present *only* when `age_state = KNOWN` — plus `static_account_count`; a schema that lets a caller read a bare date is the bug this contract exists to prevent. **It also carries the alert set** (task 15), because the payload is the only channel to the owner. **`seq` is never reset across pairings** — easy to get wrong now that rotation is a local transaction and *feels* like a fresh start: a phone holding an old payload must never be handed a lower `seq`, or I6 rejects the genuine new one. The counter belongs to the daemon, not to a pairing; assert it in a test that rotates and republishes. **Rev 14 gives this task somewhere to put what it produces.** §6.3.1 said the envelope was stored and §7 had no column for it, so this row's 'serialize → encrypt → store' had no storage contract to implement. It now writes `published_envelope` (task 03): nonce, ciphertext‖tag and the four AAD fields **stored exactly as they will be served**, inserted with the `publication` row in one transaction and flipped active under the partial unique index. **Serving is a read, never a re-derivation** — recomputing the header at request time looks equivalent and is not, because any drift in how `seq` or `published_at` is rendered changes the AAD and makes a perfectly good payload fail its tag on the phone. Two ends must compute the same bytes, so only one end computes them. **Ordering is an acceptance test, because the obvious implementation fails on the *second* publication and only the second:** the previous envelope is **deleted before** the new one is inserted, in the same transaction, or the new `is_active = 1` row collides with the old one under the partial unique index — a mechanism added for safety breaking every publish after the first. Only one envelope is ever stored; `publication` rows remain as the audit trail and carry no ciphertext. |
| 19a | Pairing: `networth pair` / `networth revoke` + the app-side secure-storage sink | 19, **20** | BLOCKED | `DESIGN.md` §6.3 and **§6.3.1**. **Rev 10 made this much smaller.** Rotation and revocation are **one local SQLite transaction** — new pairing `ACTIVE`, old `revoked_at`, stored envelope dropped — so there is no `PENDING` state, no registration round-trip, no `UNCERTAIN` outcome, no suspend-publishing rule and no `SHA-256(read_token)` verifier. *(All of those existed to survive a network call to a third party mid-rotation; earlier revisions promised "atomic" and "immediate" on a store that offered neither, then built a state machine to cope. One process, one database, one writer: "immediately" is now literal.)* The QR carries the payload key, the `pairing_id` and the VPS's tailnet name — **no read token**, because tailnet membership plus the payload key *are* the read credential. Acceptance criteria: `revoke` drops the served envelope in the same transaction that marks the pairing revoked, so a stolen phone cannot fetch again; `pair` prints the QR only after the transaction commits; **the ciphertext already cached on a stolen phone is beyond recall and the CLI copy must say so** — revocation stops the next fetch, never the last one. **No secret is ever compiled into the APK**; rotation is a re-pair, not a rebuild. App side stores via `flutter_secure_storage` (Android Keystore). |
| 20 | **The daemon's two HTTP routes, in two units** + publication-freshness monitoring | 19, **28** | BLOCKED (28) | `DESIGN.md` §16, §8.4, §8.4a, §6.4. **Rev 10 replaced six third-party routes with two local ones; rev 14 puts them in two processes and makes the public one actually deployable.** **(a) `GET /snapshot` in `networth-serve`, bound to the tailnet interface only**, database **read-only** — serves the active `published_envelope` row when its pairing is `ACTIVE`, `404` otherwise, reassembling the §6.1 header from stored columns without re-encrypting. **A bind-address regression is the one mistake here that silently publishes a private endpoint, so it is an acceptance criterion with a test, not a config comment:** assert the listener is not on `0.0.0.0`. **(b) `POST /hook/<random>` in `networth-hook`, its own unit.** *The split is the blocker fix:* one process cannot both write webhook events and hold the database read-only, and since rev 10 the phone is the only channel any alert reaches the owner through (§11) — so a parser reachable from the internet sharing a process with the phone's route is a public input with a path to silencing him. Separate units make **'no request to `/hook` can take down the snapshot server'** arithmetic rather than a promise about `except` blocks; the catch-all boundary stays as the second line. **The endpoint has to exist, and rev 13's did not.** A public IPv4 is not an HTTPS endpoint, and the 'rate-limiting reverse proxy' it leaned on was specified nowhere. Use **Tailscale Funnel**: a real domain, a certificate Tailscale provisions and renews, **no public port on the VPS at all**, and therefore no ACME client, no renewal timer and no expiry alert. Rate limiting moves **into the process** (size cap before parsing, token bucket). Fallback if Plaid rejects a `.ts.net` URL or Funnel is unavailable: nginx on the public IPv4 with Let's Encrypt over `<ip-with-dashes>.sslip.io`, which costs a second open port and a renewal lifecycle — strictly more surface, hence second. **Neither is asserted: the acceptance test is a live Sandbox webhook** (`/sandbox/item/fire_webhook`) arriving, verifying, and landing a `webhook_event` row. Until that passes, the endpoint is a plan. **A second test asserts what Funnel must *not* expose:** it publishes a port, so aiming it at the wrong process would put `GET /snapshot` on the public internet through a door the bind-address guard does not watch — assert `https://…ts.net/snapshot` returns nothing while the tailnet address still serves it. **Wiring, which rev 13 had wrong twice:** Item webhooks are set by the `webhook` field of `/link/token/create` and changed with `/item/webhook/update` — **not** in the dashboard. The URL is config (`/etc/networth/networth.env`); `link.sh` includes it when set. **This task is not complete until every Item created before it existed has been through `/item/webhook/update` and `doctor` reports zero Items without a webhook.** That backfill is what makes it safe for 08 to link long before this lands. Verify ES256 via `/webhook_verification_key/get` (cached by `kid`); compare `request_body_sha256` in **constant time against the raw received bytes** — the hash is whitespace-sensitive, so a handler that parses and re-serializes before hashing breaks every signature and makes healthy traffic look like an attack. Check `iat` against receipt time. An unverified event never reaches the state machine; `UNIQUE(body_sha256, jwt_iat)` makes Plaid's retries a no-op; `SQLITE_BUSY` returns 5xx and Plaid retries. **The unguessable path is a nuisance filter only — nothing may treat knowledge of it as authorisation.** **Advisory only — a dropped webhook must never change the number, only delay a warning.** Monitoring is evidence-based ('the last successful publication is older than expected'), never expiry arithmetic. Behind the `Publisher` seam, which survives on purpose: this joint has been swung three times. |
| 21 | Flutter app skeleton: fetch → verify `seq` → decrypt → local cache → headline **with both staleness dimensions** | 19 (+ fixture transport) | BLOCKED (design PR) | **Android only** — O6 is answered, so there is no iOS branch to keep open and no second delivery story to design. Read-only display; holds no Plaid token and never calls Plaid. **Fetches through a fixture-backed seam so it does not wait on 20 or 28.** Its acceptance criterion includes **I4**: there is no intermediate state in which this app renders a bare headline — that would be shippable and would be the exact lie the project refuses. **It must render all three age states** (§8.1 R3): `KNOWN` shows the date; `UNKNOWN` shows **"can't date this total — N of M accounts can't be dated"** and *no date anywhere near the headline*; `STATIC_ONLY` says so — which is the real state before the first Item is ever linked, not a theoretical one. Fixtures must include the mixed known/unknown case, because that is the one where a plausible implementation quietly prints the oldest known date. 22 deepens the treatment; it does not introduce it. |
| 22 | Full dual-staleness UI + **the alert surface** + downgrade handling | 21, 19a | BLOCKED | `DESIGN.md` §9, §11. **Rev 10 promoted this task: with alerts in-app only, this is where the product either works or quietly fails.** Two things it now owns outright. **(1) An unhealthy state must be impossible to miss on open, and a stale total must never render as a normal number** — there is no email, no push and no Mac, so an alert is seen when the owner opens the app and not before. **(2) `HOST_NOT_PUBLISHING` is how a host-side failure reaches him at all**, since the *publication overdue* alert cannot travel over the channel whose failure it reports (task 15). The two `COPY_STALE` reasons are an **exact predicate, not a vibe** (§9.1): `HOST_NOT_PUBLISHING` iff `last_fetch_success_at >= stale_after` **and** `last_fetch_attempt_at == last_fetch_success_at` **and** `last_fetch_seq == last_seq`; `CANNOT_CHECK` otherwise — so the app persists `last_fetch_attempt_at`, `last_fetch_success_at`, `last_fetch_error`, `last_fetch_seq` **and** `last_seq` as five distinct facts. Also: clock-skew `COPY_UNKNOWN`, and suppression of per-account "fresh" badges under a stale copy. **The rejected-downgrade warning (I6, §9.3) is phone-local and persistent** — it survives restarts, clears **only** when a `seq` greater than `last_seq` actually arrives (not on the next successful fetch, which would let one good response paper over an unexplained downgrade), and **never reaches the host**: a phone→host report channel would trade away the read-only asymmetry that makes a lost phone a non-event. A local notification after a background fetch is **best-effort and is a prompt to open the app, never the alert itself** — Android background execution is not dependable and nothing may be designed as though it will arrive. |
| 23 | History curve, with incomplete snapshots visually distinct | 21 | BLOCKED | Dashed/hollow for `is_complete = FALSE`. A gap in the record must look like a gap. **Reads stored snapshot totals; it must never recompute a past point from current observations** — with O4's revision log (task 13) that would silently redraw history every time the owner revalues the property, which is the deformation §12 exists to rule out. |
| 24 | Release signing + delivery discipline (**Android APK only**, O6 answered) | 20, 21, **22** | BLOCKED | Depends on 22 so a build that can collapse the two staleness dimensions — or bury an alert on a design that has no second channel — cannot be delivered, and on 20 so the delivered app has a real transport. Keystore outside the repo; **no secrets in the build** (§6.3). Signed from the first delivered build — a debug→release signature change later forces an uninstall. Version bump per `AGENTS.md`. |

## Phase 5 — operations

| # | Task | Deps | Status | Notes |
|---|---|---|---|---|
| 25 | ~~DB backup/restore~~ | — | **SUPERSEDED by 03a** | Number retained so review references stay valid. Moved into Phase 1 and made a gate on 08: backups scheduled *after* Production linking protect nothing during the window that matters. |
| 26 | Item budget tracking + surfacing remaining slots | 04, 08 | BLOCKED | Running out is invisible until it isn't (F2). Reads `replaces_item_id` so a replacement's cost is visible. |
| 27 | Vest-date nudge to re-confirm a manual share count | 13, 15 | BLOCKED | Manual quantities drift silently — the same failure this project exists to prevent, arriving from the manual side. |
| **28** | **VPS provisioning + hardening** (new in rev 10) | **00a** | BLOCKED (00a) | `DESIGN.md` §15.1, §13, §19 step 3. Idempotent provisioning of the owner's existing Vultr host: key-only SSH (`PasswordAuthentication no`), **a firewall opening only SSH** *(rev 14: the webhook arrives over Funnel, so the second port rev 13 opened is gone — §8.4a)*, unattended security upgrades, a **dedicated unprivileged service user**, the Python runtime, and the **three** systemd units of task 16. **Four acceptance criteria that are about not breaking the owner's machine, and they matter more than the hardening itself:** **(1) The provisioning script must not modify `PermitRootLogin` at all.** *(Rev 14 — and this is a re-fix, which is the interesting part: rev 12 identified the defect and corrected §15.1, but left `DESIGN.md` §19 step 3 — the procedure the owner actually executes — still saying the script applies `PermitRootLogin no`. A correction that lands only in the rationale and not in the executable step has not been made.)* This is **not a fresh host**: it was his Tailscale exit node before this project and he administers it as `root`. Required ordering: a non-root sudo account with his key exists → **verified working from a second, still-open session** → the script *prints the proposed change* for him to apply himself. If he declines, that is recorded, not worked around. **(2) `chown` `/etc/networth/` to the service user, keep `700`/`600`, and report it** — root-owned today because he created it, and a step that quietly adjusts permissions on the file holding the master credential is indistinguishable from one that quietly widens them. **(3) Install both credential files** — `plaid.env` and `plaid-sandbox.env` — and **require `NETWORTH_ENV` with no default**, selecting credential file, items file and database path together, asserting the file's `PLAID_ENV` agrees and refusing to start if it does not. Invent no second location; never hardcode the environment. **(4) Publish the webhook route with `tailscale funnel`** and record the resulting URL in `/etc/networth/networth.env`. Nothing is registered in the Plaid dashboard. **The concentration is accepted, not ignored:** this host is already the owner's exit node and now also holds the Plaid master credential, every `access_token` and the payload key — he was told and accepted (§15.1), and the residual is stated rather than mitigated away: **this design does not defend against compromise of the host.** Blocked on 00a only for *access* — the owner installs the agents' keys himself, and **no agent may ever ask him for a password**. Nothing but 20 depends on 28, so it must not hold up the foundation. |

## Suggested sequencing (not an assignment)

1. **First, and unchanged by either revision:** 02, 03, 04, 05, 05a. None of
   them touch a credential, a Plaid Item or the VPS. This was the foundation
   that would have survived an O2 `NO` intact; O2 said **GO**, so nothing was
   spent on the contingency — but the property that made it safe to start before
   the answer existed is the same property that makes it the right place to
   start now. Rev 10's host move touched the transport and the scheduler, not
   the schema, the staleness machine or the snapshot model.
   **03a can be built alongside them but cannot *pass* until the owner has done
   00a.** Rev 14 narrows what is left of 00a: the tailnet half is **done and
   verified** — `zelengs-macbook-air-2` is Connected at `100.96.163.67` and SSH
   Mac→VPS works over it — so the remaining blocker is only the two
   `authorized_keys` entries. Building 03a early is right; calling it green
   before a pull has actually landed and decrypted would be the rev-2 mistake
   again.
2. **00 is done and O2 said GO**, so this leg is ungated: 06 → 07 → 08, with
   **08 still gated on 03a's drill passing from the real destination**. Note what
   O2's GO does *not* cover: a directory listing proved the credential, Trial
   Production access and VPS egress, and proved nothing about any particular
   institution — the first per-institution evidence costs a lifetime slot to
   obtain (**F2a**), which is why 06's Sandbox rehearsal carries more weight than
   its position in this list suggests. That gate
   is not an open question and does not clear with O2 — the slots are lifetime
   regardless of plan. Rehearse in Sandbox before spending a single Production
   slot; Sandbox is free and unlimited, Production mistakes are permanent.
3. **The core value:** 10 → 11 → 12 → 14 → 15 → 16, with 12b close behind 09.
   Task 11 deserves more review attention than anything else in this list — it
   is where a successful API call is prevented from masquerading as fresh data.
4. **Then the number travels:** 17 → 18 → 19 → **28 → 20 → 19a**, and 21 → 22
   in the app. Two ordering notes, both changed by rev 10:
   - **28 comes first now.** 20 is two routes *on the VPS*, so the host has to
     exist and be hardened before there is anywhere to run them. 28 is the only
     task gated on owner access (00a), which is why it sits here rather than in
     Phase 0 where it would look like it blocked everything.
   - **20 still precedes 19a, but for a weaker reason than before.** Pairing used
     to be a conversation with a third party, so the transport genuinely had to
     exist first; now it is a local transaction and the dependency is only that
     `pair` should be able to print a QR for a server that is actually running.
     Do not read it as the old constraint.
   21 runs against fixtures in parallel and does not wait for any of it.
   **22 is not polish** — it is invariant I4 *and*, since rev 10, the only
   surface any alert reaches the owner through (§11). 21 ships the minimum of it.
5. **Last:** 23, 24, and Phase 5.
