"""Task `06a`'s measurement rigs.

Nothing here is production code. Each module in this package exists to *measure*
a behaviour that `07a`, `07b` and `08` are written against, so those tasks
implement something observed rather than something assumed.
"""
